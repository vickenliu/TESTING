### new react route.

react-router-dome has 'staticRouter' and 'browserRouter' to wrap the App

for react to be rendered on the server, staticRouter is used, it accepts location(url) and context (the props for the matched component)

//To do: update the reddit-wlg's server reder routed react page.
